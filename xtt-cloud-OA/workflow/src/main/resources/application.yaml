server:
  port: 8091

spring:
  application:
    name: workflow
  cloud:
    nacos:
      discovery:
        server-addr: nacos-server:8848
        group: xtt-cloud-oa
      config:
        server-addr: nacos-server:8848
        group: xtt-cloud-oa
        file-extension: yaml
  config:
    import:
      - optional:nacos:workflow.yaml
#      - optional:nacos:datasource-config.yaml

