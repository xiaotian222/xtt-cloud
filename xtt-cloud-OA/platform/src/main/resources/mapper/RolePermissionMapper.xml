<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="xtt.cloud.oa.platform.domain.mapper.RolePermissionMapper">

    <resultMap id="PermissionResultMap" type="xtt.cloud.oa.platform.domain.entity.Permission">
        <id column="id" property="id" jdbcType="BIGINT"/>
        <result column="code" property="code" jdbcType="VARCHAR"/>
        <result column="name" property="name" jdbcType="VARCHAR"/>
        <result column="type" property="type" jdbcType="VARCHAR"/>
        <result column="created_at" property="createdAt" jdbcType="TIMESTAMP"/>
        <result column="updated_at" property="updatedAt" jdbcType="TIMESTAMP"/>
    </resultMap>

    <select id="selectPermissionsByRoleId" parameterType="java.lang.Long" resultMap="PermissionResultMap">
        SELECT p.id, p.code, p.name, p.type, p.created_at, p.updated_at
        FROM sys_permission p
        INNER JOIN sys_role_permission rp ON p.id = rp.permission_id
        WHERE rp.role_id = #{roleId}
        ORDER BY p.created_at ASC
    </select>

    <select id="selectRoleIdsByPermissionId" parameterType="java.lang.Long" resultType="java.lang.Long">
        SELECT role_id
        FROM sys_role_permission
        WHERE permission_id = #{permissionId}
    </select>

    <insert id="insertRolePermission">
        INSERT INTO sys_role_permission (role_id, permission_id)
        VALUES (#{roleId}, #{permissionId})
    </insert>

    <delete id="deleteRolePermissions" parameterType="java.lang.Long">
        DELETE FROM sys_role_permission WHERE role_id = #{roleId}
    </delete>

    <delete id="deletePermissionRoles" parameterType="java.lang.Long">
        DELETE FROM sys_role_permission WHERE permission_id = #{permissionId}
    </delete>

    <insert id="insertRolePermissions">
        INSERT INTO sys_role_permission (role_id, permission_id)
        VALUES
        <foreach collection="permissionIds" item="permissionId" separator=",">
            (#{roleId}, #{permissionId})
        </foreach>
    </insert>

</mapper>
